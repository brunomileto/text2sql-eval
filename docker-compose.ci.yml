# docker-compose.ci.yml — local CI pipeline definition
services:
  ci:
    build:
      context: .
      dockerfile: Dockerfile.ci
    environment:
      - CI=true
    command: >-
      sh -c "
      make check
      "
