# RAG Corpus

This directory stores curated source documents for the reusable RAG subsystem.

Rules for v1:

- keep files human-authored and repo-managed
- use plain text or Markdown files
- treat this as source content, not generated output
- rebuild the RAG index after changing files here
